<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">
    <select id="list" resultType="com.example.demo.entity.User">
        select * from user
    </select>

    <select id="detail" parameterType="int" resultType="com.example.demo.entity.User">
        select * from user where id = #{id}
    </select>

    <update id="update" parameterType="com.example.demo.entity.User">
        update user set name=#{name}, pid=#{pid}, url=#{url} where id = #{id}
    </update>

    <insert id="submit" parameterType="com.example.demo.entity.User">
        insert into user (name, pid, url) VALUES (#{name}, #{pid}, #{url})
    </insert>

    <delete id="remove" parameterType="int">
        delete from user where id = #{id}
    </delete>
</mapper>